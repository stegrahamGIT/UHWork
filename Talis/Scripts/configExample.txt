# Token URL e.g. https://users.talis.com/oauth/tokens
tokenURL::
# OAuth Client ID and Secret
# see https://support.talis.com/hc/en-us/articles/208221125-Using-Talis-JSON-APIs
clientID::
secret::
# Talis GUID for user who is updating records
talisGUID::
# File that contains the list of Items IDs that we want to update
itemIDFile::
# tenancy URL e.g. https://rl.talis.com/3/<ID>/
baseURL::
# Only used in the logs, so URLs are usable
webBaseURL::https://<ID>.rl.talis.com/
# String in URL to match on e.g. shibboleth2sp.gar.semcs.net
matchString::
# The new URL string: http://www.vlebooks.com/vleweb/product/openreader?id=Herts&isbn=
replacementURL::
# The regular expression pattern you want to capture the info with
# needs to contain a back reference
# e.g. https.*isbn%25253D(.*)
regexPattern::
